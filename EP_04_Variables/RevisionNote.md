# Quick Revision — Variables

* `const` default; prevents rebinding, not mutation.
* `let` block-scoped; allows reassignment.
* `var` function-scoped; hoisted to `undefined` and creates global property in browser.
* TDZ: `let`/`const` throw if accessed before declaration.
* Use `const` most of the time; avoid `var`.
