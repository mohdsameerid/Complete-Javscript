# Quick Revision — Arrays

* store multiple values
* index starts from 0
* push/pop = end
* shift/unshift = start
* map = transform
* filter = select
* reduce = single value
* slice = copy, splice = change
